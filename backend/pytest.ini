[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: fast isolated tests (no network, no DB server)
    integration: full-stack tests through the ASGI app
addopts = -q --cov=app --cov-report=term-missing --cov-fail-under=85
